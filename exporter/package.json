{
  "name": "lorf-telemetry-exporter",
  "version": "1.0.0",
  "description": "Exports Claude Code telemetry to Supabase for the LORF operations dashboard",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "backfill": "bun run index.ts --backfill",
    "open": "bun run lorf-open.ts",
    "close": "bun run lorf-close.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2"
  }
}
